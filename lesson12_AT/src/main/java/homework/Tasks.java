package homework;

import org.junit.jupiter.api.Test;

import java.sql.SQLException;

public class Tasks {
    @Test
    void task1() throws SQLException {
        /*
            Напишите SQL Query для таблицы variant5task1, которая удаляет запись с значение 'ivan' в поле username
         */

    }

    @Test
    void task2() throws SQLException {
        /*
        Напишите FULL OUTER JOIN with null для таблиц variant5task2first и variant5task2second.
        Напоминаю, этот join должен показывать только те записи, которая не совпадают в двух таблицах.
        Проверьте, что в ответе нет значения 'tolik' для полей username и client.
         */

    }

    @Test
    void task3() throws SQLException {
        /*
        Напишите SQL Query для таблицы variant5task3, которая вернёт запись с значение 10 в поле age.
        Проверьте, что такая запись есть.
         */
    }
}